# Docs

Project documentation lives here.

Current contents:

- `CrewAI_Job_Application_Copilot_MVP_Docs.md`
- `crewai-job-copilot-mvp-docs/`
- `project-audit/` — the 2026-07-10 repository, production, product, UX,
  security, monetization, and roadmap audit. Start with
  `project-audit/00-executive-summary.md`.

The CrewAI-named documents describe the original product concept and historical
compatibility. The active runtime uses deterministic analysis plus LangGraph for
the durable approval workflow; OpenClaw remains a separate private integration.

The canonical working context remains at the repository root in `Context.md` because the project process requires that file to be read before implementation and updated after every meaningful change.
